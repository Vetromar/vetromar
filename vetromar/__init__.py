"""Vetromar v0: capture tool + platform store.

The store is the spine; the capture tool is the wedge that writes into it;
a thin MCP server is the read surface the customer's agent queries.
"""

__version__ = "0.5.0"
